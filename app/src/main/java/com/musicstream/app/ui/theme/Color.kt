package com.musicstream.app.ui.theme

import androidx.compose.ui.graphics.Color

// ================= BASE PALETTE (REQUIRED) =================
val PalettePrimaryRed = Color(0xFFBDC3C7)      // Main Accent (Deep Crimson Red)
val PaletteDarkBlue = Color(0xFF2C3E50)       // Dark Background Base / Primary Brand Color
val PaletteGrey = Color(0xFFBDC3C7)           // Light Grey (Muted elements / Borders)
val PaletteOffWhite = Color(0xFFECF0F1)       // Light Background base
val PaletteWarmWhite = Color(0xFFF5F2EE)      // Soft Warm White

// ================= DARK THEME COLORS =================
// Dark Slate-Blue base colors for modern high-contrast aesthetic
val DarkBackground = Color(0xFF1E2B37)        // Dark variant of PaletteDarkBlue (#2C3E50)
val DarkSurface = Color(0xFF2C3E50)           // Base Dark Blue for primary surfaces
val DarkCardSurface = Color(0xFF34495E)       // Slightly lighter slate for nested cards
val DarkElevated = Color(0xFF3D566E)          // Border and elevation lines for Dark Mode

val TextPrimaryDark = Color(0xFFF5F2EE)       // Highly readable Soft Warm White
val TextSecondaryDark = Color(0xFFBDC3C7)     // Soft Muted Grey for descriptions
val TextTertiaryDark = Color(0xFF7F8C8D)      // Darker grey for metadata

// ================= LIGHT THEME COLORS =================
// Light warm palette with clear hierarchy and structural boundaries
val LightBackground = Color(0xFFF5F2EE)       // Soft Warm White background base
val LightSurface = Color(0xFFECF0F1)          // Soft grayish surface
val LightCardSurface = Color(0xFFFFFFFF)      // Crisp pure white for cards
val LightElevated = Color(0xFFBDC3C7)         // Light Grey used for structural elevation

val TextPrimaryLight = Color(0xFF2C3E50)      // Deep Slate Blue for sharp typography contrast
val TextSecondaryLight = Color(0xFF5A738E)    // Medium slate for secondary elements
   // Standard muted helper text

// ================= SHARED ELEMENTS =================
val FavoriteRed = Color(0xFFE74C3C)           // Vibrant accent for likes/favorites
val InactiveGrey = Color(0xFF95A5A6)          // Standard disabled/inactive control state

// ================= ACCENT COLORS =================
val AccentCyan = Color(0xFF06B6D4)
val AccentOrange = Color(0xFFF97316)
val AccentPurple = Color(0xFFBDC3C7)
val AccentAmber = Color(0xFFFBBF24)
val AccentGreen = Color(0xFF10B981)